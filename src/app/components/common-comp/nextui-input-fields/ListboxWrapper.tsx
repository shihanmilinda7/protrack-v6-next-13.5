import React from "react";

export const ListboxWrapper = ({ children }) => (
  <div className="border-small px-1 py-2 rounded-small border-blue-400 dark:border-default-100 bg-gray-100">
    {children}
  </div>
);
